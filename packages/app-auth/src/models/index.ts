import * as loginModel from './loginModel';
import * as registerModel from './registerModel';
import * as appSettingModel from './appSettingModel';
import * as forgetModel from './forgetModel';

export default {
  loginModel,
  registerModel,
  appSettingModel,
  forgetModel,
};
