import { getSafetyObjectValue } from './/getObject';
import { runFunction } from './runFunction';
import {
  DataFormatMapType,
  transformKeySubmitValue,
} from './transformKeySubmitValue';

export type { DataFormatMapType };
/**
 * 统一导出
 */
export { runFunction, getSafetyObjectValue, transformKeySubmitValue };
